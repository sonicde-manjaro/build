# build

This repository hosts the workflows necessary to build all packages of this organization.

## Git Configuration

This repository includes a shared Git configuration in .gitconfig. To enable it for your local clone, run the following command once from anywhere inside the repository:

```
git config --local include.path ../.gitconfig
```

This enables repository-specific merge rules without modifying your global Git configuration.
